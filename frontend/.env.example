# The full URL of your deployed backend (no trailing slash).
# On Netlify: set this as an environment variable in Site Settings → Environment Variables.
# Leave empty for local development (Vite proxy handles it automatically).
VITE_API_URL=https://your-railway-backend-url.railway.app
