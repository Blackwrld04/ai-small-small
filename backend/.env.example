PORT=4000
JWT_SECRET=change_this_to_a_long_random_string_at_least_32_chars

# Comma-separated list of allowed frontend origins (no trailing slash, no spaces).
# On Railway: set this to your Netlify site URL.
# Example: ALLOWED_ORIGINS=https://your-site.netlify.app
# For multiple origins: ALLOWED_ORIGINS=https://your-site.netlify.app,https://your-preview.netlify.app
ALLOWED_ORIGINS=http://localhost:5173
